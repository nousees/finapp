{
  "name": "finapp-integration-tests",
  "version": "1.0.0",
  "description": "Integration tests for FinApp API",
  "scripts": {
    "test": "jest api.test.js",
    "test:watch": "jest api.test.js --watch",
    "test:verbose": "jest api.test.js --verbose"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "verbose": true,
    "testTimeout": 30000
  }
}
