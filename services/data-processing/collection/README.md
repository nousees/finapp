• Принимает данные от клиента (голос, импорт CSV, ручной ввод)
• Валидирует
• Сохраняет сырые транзакции в БД
• Отправляет аудио в ML (если голос)
• Вызывает категоризацию (если нужно сразу)

POST /transactions
POST /import
POST /voice/upload